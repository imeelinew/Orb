# Copy to subtitle-secrets.env (same directory) or
# ~/Library/Application Support/Orb/subtitle-secrets.env
# Both paths are gitignored; never commit the real key.
LLM_OPENROUTER_API_KEY=sk-your-key-here
